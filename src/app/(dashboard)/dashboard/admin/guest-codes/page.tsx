import { GuestCodeManager } from "./_components/guest-code-manager";

export const metadata = {
  title: "Guest Codes - Property Manager",
};

export default function GuestCodesPage() {
  return <GuestCodeManager />;
}
